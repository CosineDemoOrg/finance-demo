---
# This is a TEMPLATE for the JWT signing key Kubernetes Secret.
# DO NOT commit real keys to version control.
# Generate a fresh keypair using: python3 scripts/generate_jwt_keys.py
# The old committed keypair has been removed for security (see C-1).
---
apiVersion: v1
kind: Secret
metadata:
  name: jwt-key
type: Opaque
data:
  # jwtRS256.key: <BASE64-ENCODED-PEM-PRIVATE-KEY>
  # jwtRS256.key.pub: <BASE64-ENCODED-PEM-PUBLIC-KEY>
